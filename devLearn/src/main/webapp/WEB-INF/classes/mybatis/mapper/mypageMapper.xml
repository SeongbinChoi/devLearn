<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="myWrite">

	<!-- 커뮤니티 qna 작성글 개수 -->
	<select id="qnaDataCount" parameterType="map" resultType="Integer">
		SELECT NVL(COUNT(*), 0) 
		FROM communityQna
		WHERE memberEmail=#{memberEmail}
	</select>

	<!-- 커뮤니티 qna 작성글 리스트 -->
	<select id="qnaList" parameterType="map" resultType="com.sp.dev.mypage.myWrite.MyWrite" >
		SELECT qnaNum, memberEmail, subject, content, hitCount, regDate, groupNum, 
				depth, groupOrder, parent, replyNum, selected
		FROM communityQna
		WHERE memberEmail=#{memberEmail}
	</select>
</mapper>